# L-483 修正内容メモ

対象: https://www.dairaku-accfirm.com/
ファイル: index.html
修正日: 2026-03-30

## 修正概要

既存の HTML 要素を削除せず、CSSは `<style>` ブロックで上書き、新規要素はファイル末尾に追加。

### 修正1: 固定お問い合わせバー（CTA改善）
- 画面下部に `position: fixed` で常時表示
- 電話番号ボタン（📞 03-5797-9487）とお問い合わせボタン（赤）
- お問い合わせボタンはコンタクトフォームへリンク
- モバイル対応（600px以下でサイズ調整）
- バッジ: ✨ 改善1

### 修正2: セクション見出しのリデザイン（デザイン改善）
- h2（h2-E009-blue）に左ボーダー4px + 下ボーダー2px を追加
- h3（h3-E009-blue）に左ボーダー3px + 下ボーダー1px を追加
- 最初の h2 見出しにバッジをJS で挿入
- バッジ: ✨ 改善2

### 修正3: お知らせ欄の見やすさ改善（UX改善）
- テーブル行にストライプ背景（偶数行: #f7f9fc）
- セルに余白（padding: 12px 10px）追加
- ホバー時にハイライト（#eef3fa）
- 「お知らせ」見出しにバッジをJS で挿入
- バッジ: ✨ 改善3

### デモバナー
- `<body>` 直後に `position: relative` で配置（非固定、スクロールで流れる）
- テキスト: 「修正イメージデモ — 株式会社CAPSULE」

## 技術的な実装

- CSS: `<body>` タグの直前に `<style>` ブロックを挿入（既存CSS上書き用）
- HTML: ファイル末尾に sticky footer + バッジ挿入スクリプトを追加
- z-index: デモバナー(100000) > sticky footer(99999)
- モバイル対応: `@media (max-width: 600px)` でレスポンシブ調整

## バックアップ

- original.html: 修正前のバックアップファイル
